git init Project (to creat top level directory caled project)
tree .git (there is ono and only .git for each repository)

Notepad first.txt (you can try Brackets first.txt OR code first.txt)

git status
(first.txt will be marked with red color )

git add first.txt

git status
(first.txt now will be marked with green color )

git commit -m "My first commit"

git remote remove origin

git remote add origin https://github.com/MallickGabriel/GithubTestDreive.git
git push -u origin master

…or create a new repository on the command line
echo # GithubTestDreive >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/MallickGabriel/GithubTestDreive.git
git push -u origin master


I need to make sure that every thing is updated


























		